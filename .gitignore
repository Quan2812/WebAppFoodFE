# Bỏ qua thư mục node_modules (rất quan trọng)
node_modules/

# Bỏ qua thư mục build (nếu bạn đã build dự án)
build/

# Bỏ qua các file môi trường (nếu chứa thông tin nhạy cảm)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Bỏ qua các file tạm thời của IDE
.vscode/
.idea/

# Bỏ qua các file log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Bỏ qua file lock nếu bạn không dùng yarn
# yarn.lock (nếu bạn dùng npm)
# hoặc bỏ qua package-lock.json nếu bạn dùng yarn
# package-lock.json (nếu bạn dùng yarn)
# Bỏ qua file __MACOSX
__MACOSX/